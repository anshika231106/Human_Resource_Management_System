services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_DB: dayflow
      POSTGRES_USER: dayflow_admin
      POSTGRES_PASSWORD: dayflow_pass
    ports:
      - "5544:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: